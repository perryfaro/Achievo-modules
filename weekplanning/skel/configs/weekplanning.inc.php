<?php

  /**
   * If set to true a user with no contract will
   * never cause a conflict. If set to false the
   * user with no contract will always cause a
   * conflict.
   */
  $config["no_contract_no_conflict"] = false;

?>
